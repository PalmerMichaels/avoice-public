{
  "name": "avoice",
  "version": "0.1.0",
  "description": "Clean-room TypeScript CLI for synthetic voice queue routing experiments.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/cli.ts",
    "test": "node --test tests/*.test.ts",
    "validate": "node scripts/validate.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {}
}
