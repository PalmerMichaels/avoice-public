{
  "name": "avoice",
  "version": "1.0.0",
  "description": "Clean-room synthetic architecture workflow CLI.",
  "type": "module",
  "bin": {
    "avoice": "dist/src/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/src/index.js",
    "test": "npm run build && node --test dist/test/*.test.js",
    "validate": "npm test && node dist/src/index.js"
  },
  "keywords": [
    "clean-room",
    "typescript",
    "cli",
    "synthetic-data",
    "architecture-workflow",
    "workspace-planning"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.11",
    "typescript": "^5.5.4"
  }
}
